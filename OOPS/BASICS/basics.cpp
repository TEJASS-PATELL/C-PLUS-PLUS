#include<iostream>
using namespace std;
   
int main(){
     // oops -> classes and object
     // c++ -> initally called C with classes
     // classes -> extension of structure (in C)
     // structure has limitation -> members are public
     // No methods
     // classes = structures + more
     // classes -> can have methods and properties
     // classes -> can make few members as private and few as public
     // structures in c++ are typedef
     // you can declare objects along with the class declaration like this:
     /* class employee{
          class declaration
     }a,b,c; */
     // a.salary = 90000 makes no sense if salary is private
}